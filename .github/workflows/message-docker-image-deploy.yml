name: Build and Push Message service Docker Image

on:
  push:
    branches:
      - master
    paths:
      - "src/services/Message/src/**"
      - "src/shared/**"
      - ".github/workflows/message-docker-image-deploy.yml"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./deployment/services/message/Dockerfile
          push: true
          tags: trogdev/quickchat-message-service:latest
